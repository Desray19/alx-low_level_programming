#include "main.h"
/**
 * mul - checks wheather a character is a digit (0-9)
 * @a: the first number
 * @b: the second number
 *
 * Return: the multiple of the two iningers
 */
int mul(int a, int b)
{
int multiple = a * b;
return (multiple);
}
