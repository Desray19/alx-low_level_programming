#include "main.h"
/**
 * _abs - returns the absolute value of a number
 * @n: the number to be checked
 *
 * Return: a positive number
 */
int _abs(int n)
{
if (n == 0)
{
return (n);
}
else if (n > 0)
{
return (n);
}
else
{
return (n * -1);
}
}
